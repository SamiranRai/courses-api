spring:
  application:
    name: courses-api

  datasource:
    url: jdbc:mysql://mysql-db:3306/courses_api
    username: user
    password: RAY123sam
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
